{
  "name": "live-timing-dashboard",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "PUPPETEER_CACHE_DIR=/opt/render/project/src/.puppeteer-cache npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "PUPPETEER_CACHE_DIR=/opt/render/project/src/.puppeteer-cache node server/index.js"
  }
}
